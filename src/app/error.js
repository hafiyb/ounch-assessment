'use client';

const Error = (e) => {
  return (
    // simple error page, improve later
    <div className='flex flex-col h-screen w-full justify-center items-center'>
      <p>Error, please try refreshing page</p>
    </div>
  );
};

export default Error;
